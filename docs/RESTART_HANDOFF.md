# Furnio Mobile — restart handoff

## Latest checkpoint — Apple listing and Android account switching

Apple activation and agreement are resolved. Explicit `ai.furnio.app` App ID registered with Push Notifications and Sign In with Apple. Furnio ASC record **6810452958**, SKU `furnio-ios-001`, iOS/English (U.S.), **1.0 Prepare for Submission**, manual release saved. Subtitle/categories saved; see `STORE_SUBMISSION_STATUS.md`. Do not duplicate the record or resume RevenueCat. Existing browser tab7 is on App Information and should be retained.

**Both Apple signing certificates are now complete and verified.** Owner's latest screenshot shows one Development and one Distribution certificate; `security find-identity -v -p codesigning` confirms **2 valid identities**, matching team **5SY24C9RBH**. These are not duplicate certificates. Do not ask for another click/permission or create/revoke another identity. Read `IOS_SIGNING.md` for public fingerprints/validity and the resolved WWDR G3 chain issue. No private keys exported, trust overrides, search-list or network changes.

Next signing/release work is provisioning/profile and store-build preparation, not certificate creation. No profile/archive/upload yet. Team ID is saved in app.config.ts, TS + four commerce-policy tests pass, generated iOS project not yet updated. Xcode 27 RC is available but not installed; do not alter global Xcode/network settings or call beta QA output a store binary. Real-service/privacy/device release gates still apply.

Android actual staging tests now pass: synthetic owner sign-in, 20-credit wallet, project detail, force-stop/relaunch session recovery, sign-out, second-account sign-in and empty projects. The second account's Home and Projects show no prior owner's project. Evidence in `ANDROID_NATIVE_QA.md`; current emulator-5554 is on **other** account Projects. No fresh build or paid/outbound test performed. Do not repeat accepted iOS/Android milestones. Remaining OAuth/SMS/imaging/deletion/push/physical-device and store gates remain open. No production change/push/deploy; Mobile remote absent.

## Historical handoff — 9 September, 19:51 Toronto (superseded above)

Read the top of `PROGRESS.md`, `WEB_PURCHASES_DECISION.md` and `STORE_SUBMISSION_STATUS.md`. Native SDK removal and legacy website billing adapter are DONE and tested; do not repeat the previous pending cleanup. Both latest native staging builds succeeded and were installed; iPhone Keychain startup/sign-in/wallet/project/cold-start/sign-out/account-switching passed. iPhone currently uses the synthetic **other** customer (empty projects). Latest APK hash/logs are in Progress. Android owner authorized **ADB UI controls on emulator-5554 only** because CUA cannot access the standalone qemu window. `uiautomator dump` exits137; use ADB screenshot + input, not retrying that broken dumper. Synthetic owner credentials were entered from protected fixture file; Sign in button just tapped. Inspect actual outcome before claiming acceptance.

Only customer staging Worker redeployed (version `a04e1d98-7e05-4a38-9e19-6258bf50e892`) for GET billing; 10 hosted checks pass, no paid/outbound operations. No production migration/push/deploy. No build process remains running. Mobile remote remains absent.

Owner created Google Furnio app in Realview Hub account (ID/listing/link in store document). Production access requires 12 testers for 14 continuous days; none enrolled yet. Owner logged into Apple, but ASC says account isn't enabled; owner confirmed no membership and asked how to activate. Official enrollment page is open in browser tab6; needs owner identity/agreement/payment, not agent financial action. Google tab5 preserved. Latest user question is enrollment instructions; answer simply, then continue independent QA when resumed. Do not resume RevenueCat setup, promise submission before prerequisites or enable destructive deletion without completion/retention decisions.

## Current handoff — website-only companion, 9 September 19:27 Toronto

Owner approved website-only purchases. Follow `WEB_PURCHASES_DECISION.md` first: RevenueCat/store products and native payment rollout are no longer v1 tasks. Wallet is read-only, refreshes on entry/foreground, and has no purchase links. Store operations are guarded off even with stale true environment flags. **1,057 tests / 59 files, TypeScript, frozen contracts and diff checks pass.** This revision has not been rebuilt/visually tested on native devices. The previous ad-hoc iOS staging build has not been certified; don't infer a successful Keychain fix. Preserve deferred commerce work and staging, keep production Stripe/credit rules unchanged, finish real integration/privacy/device acceptance. Native SDK/generated-project release cleanup and moving production wallet reads off deferred native-billing functions remain explicit follow-ups. No production database/Worker change, deployment or push this revision.

## Superseded handoff

## Current handoff — 9 September 19:05 Toronto

Read `STAGING_INTEGRATION.md` first. Staging approval/sign-in/schema/basic authenticated API work are complete: 59 migrations, 19 hosted SQL checks, nine grouped API checks, separate KV and two Workers. Production is unchanged; paid/outbound/deletion gates remain closed. Staging-native build support is implemented with public-only configuration. iOS/Android build/visual acceptance is the active next step. Owner signed in to RevenueCat and authorized completing its setup. Open Google Play for correct-account selection when needed. No Mobile Git remote exists. Ignore all older instructions below to pause, obtain staging approval, install Android software or recreate staging.

## Historical handoff notes

**Latest — staging creation complete:** owner signed in; verified Pro org/Micro additional $10/month. Created `Furnio-Mobile-Staging`, ref `bcrobmrimzkvfrnqarmv`, org `mtbmdoltmytsmmpuklpw`, us-east-1, healthy/PG17.6.1.166. Dashboard SELECT returned 0 Auth users/Storage objects/public tables. Generated password/ref/URL are only in ignored mode0600 `.env.staging.local`; never print/commit/load into Expo. No schema, keys, runtime flags, Workers or live data changed. Main remains linked to production. Follow `STAGING_SETUP.md` next for complete schema and test-only setup, including proper provider CA for strict-TLS pooler connection (system-root check failed; do not disable verification/change DNS). **Staging approval and sign-in are resolved.** Earlier holds below are historical. App release remains incomplete.

**Current owner response supersedes the approval hold below:** owner explicitly approved creating `Furnio-Mobile-Staging` in their Supabase account. CLI confirms organization `mtbmdoltmytsmmpuklpw` (`alipashaamidi's Org`) contains Furnio-Production and no Furnio staging project. Supabase browser is at sign-in, shown to owner for billing-plan verification. Project creation is authorized but NOT yet performed. After sign-in, verify the Micro price/organization and create the separate project using `STAGING_SETUP.md`; do not ask for the same staging approval again. Keep production and its Main CLI link untouched. Other retention, store and actual integration gates remain unresolved.

**Owner-input hold after the completed local milestones:** the same staging approval remains unanswered across successive goal turns. Current checks confirm no mobile Git remote, no `.env`/`.env.local`/backend `.dev.vars` staging configuration, and final deletion still explicitly blocked by `deletionProcessorImplemented = false`. Published policies do not supply the missing financial/backup retention schedule. The remaining integrated release path requires owner-approved staging and account/policy decisions. Stop automatic continuation instead of repeating accepted tests or creating speculative work. Resume when the owner approves dedicated Furnio staging (or identifies an existing isolated one), then follow `STAGING_SETUP.md`. Do not mark the app complete or enable deletion/purchases/production flags. No further laptop software is currently required for the verified local builds.

**Newer iOS dependency checkpoint — 18:12 Toronto:** the upstream404 workaround is retired locally. Official Core/Dependencies Debug/Release archives are available and exactSHA256 matches. Scoped update of only two pinned podspecs restores HTTPS sources/configuration phases; all pod versions unchanged. Release build/framework signatures/exact-install startup pass. Read `IOS_DEPENDENCY_READINESS.md` for four beta compile diagnostics with exit0, warnings and no clean-CI/store claim. Buildlog `output/ios-simulator-2026-09-09T22-08-17.383Z.log`, installed JS `e45a26944c3e40c80f9193205aeb9ebbe4c18f2bbe55186f664f747f4676beb4`; sample welcome open in existing QA simulator, PID18181 at last check. No new tool/server/port, production change, SQL/Worker change, push or paid infrastructure. **No staging approval has arrived.** Do not restart builds/reapply cached-file overrides on continuation. The completed full-schema milestone below remains current.

**Authoritative latest checkpoint — 9 September, after the owner asked to continue:** full application-schema testing now replays **60 Main/Admin migrations / all25 native migrations** and passes **19 checks**. It exposed a real pending-phone native purchase rollback; new unapplied `20260909215817_native_unstarted_trial_conversion.sql` fixes only that state without phone bypass or shared conversion-function edits. Read `FULL_SCHEMA_REPLAY.md` and the top of `PROGRESS.md`. No production change/push or new native build; existing accepted iOS/Android builds stay unchanged. Source inventory123 passes. The owned RAM-only, network-none fixtures were removed; unrelated Docker projects untouched. **Hosted Furnio staging approval remains unanswered: do not create a paid Supabase project on automatic continuation.** Retention, final deletion, actual Auth/store/device integration gates remain. Older checkpoint counts and restart/pause instructions below are historical, not current.

**Newest checkpoint, 9 September 17:26 Toronto:** Android local build/mask/export milestone passed. Read the follow-up in `ANDROID_NATIVE_QA.md`: native data-URI decoder, SVG cached-image onLoad/UI-thread offscreen measurement, explicit Android font equivalents and accessible button titles fixed. Final1043tests/58files, TS/contracts, Gradle/package/bundle/signature/ZIP alignment pass. Latest installed APK `10e394e8f3bc484633155034cecea7b9e7d375780fb77f9a3fbe09c62982722e`, log `output/android-emulator-2026-09-09T21-20-36.780Z.log`. Final native serif/no-label JPEGs preserve960×640; repeat monospace export/native share/cancellation pass. Mask-fix build had already passed actual two-region PNGs/Undo. Existing iOS patch hunks preserved; no iOS rebuild was needed for Android-only native changes (shared button accessible name is additive). New font helper tests preserve iOS/web font values. Do not redo these unchanged milestones. `_2` emulator remains temporary/read-only on Home, serial5554, app PID last9961, no Metro/build server. Four diagnostic JPEGs and the original bundled QA photo are in its temporary Photos session; no external share/live call. No Main/Admin/Worker/SQL/Docker/network/production change. Mobile still has no remote, edits remain uncommitted, all23 migrations unapplied/flags disabled. Continue real staging/auth/service/commerce, private-cache/device stress, and remaining deletion/retention work; no laptop Android dependency is missing now. Owner's Docker shutdown and specific financial/backup retention choices remain unresolved; do not infer them or restart/reconfigure unrelated systems.

**Newest checkpoint, 9 September 16:54 Toronto:** read `ANDROID_NATIVE_QA.md`. Android prerequisites are now complete for the actual passing local ARM64 build, not just filesystem checks. Added only the missing base API36 platform; Gradle installed exact side-by-side Build-Tools35.0.0/CMake3.22.1 using existing licence acceptance. `scripts/build-android-emulator.mjs` builds an embedded demo APK with public environment values removed and dotenv disabled. Final APK hash `59b4610a65ff29ba98f2fa393929be47fe8de296082dae18d29e1d734a775c87`, log `output/android-emulator-2026-09-09T20-48-11.358Z.log`. System photo picking/style/confirmation/sample result checks passed on the first build; the final Android-only tab sizing/safe-area correction rebuilt and passed visual inspection. Existing iOS behaviour unchanged. No Main/Admin/Worker/SQL/deployment/push/Docker/network action; Mobile still has no remote and all production flags are off. The existing `_2` AVD is running with `-read-only -no-snapshot-save`, serial `emulator-5554`, app PID last6111, emulator process last85727 / exec session21789; re-resolve before operating. It is intentionally left on sample Home for the owner; no Metro server. Standard emulator/ADB loopback services are expected. Do not stop unrelated processes, wipe devices, repeat accepted brushing or rebuild unchanged binaries. Next work remains staging/all-service Android acceptance, physical devices/store sandbox, remaining account cleanup/retention, native compatibility and release gates. The older missing-Android statements below are historical and superseded.

**Newest checkpoint, 9 September 16:31 Toronto:** read `PHOTO_INPUT_PRIVACY.md`. Native input copies and returned SDK files now have screen/session ownership, durable markers and startup/foreground recovery. Fixed actual Expo async `File.copy` ordering in inputs, drafts and disclosure export after the first native pick failed despite mocked passes. Final **1,024 tests/57 files**, Mobile TS/contracts and beta Release/configuration build pass: `output/ios-simulator-2026-09-09T20-22-39.001Z.log`, installed JS SHA256 `15f5bf3689534ab66d26eba49461b8735431ee9c222395d26769be8fb7695e2f`. Final native Studio photo/Back cleanup, local result photo, inspected 1668×2500 labelled JPEG, cancelled-share cleanup and app-restart recovery pass. Legacy27+27 SDK files preserved; no Photos original or external share sent/deleted. Final simulator PID last83807; re-resolve all paths/PIDs. No new listener/production/SQL/Worker/Docker action. App remains demo, native purchases/deletion flags disabled, 23 mobile migrations remain unapplied to production. Only Android `platforms/android-36` is now missing (16:19 directory check); other three formerly missing SDK packages are installed. No Mobile remote. Owner's Docker shutdown choice and payment/backup retention schedule remain unresolved; policy links do not supply periods. Do not redo accepted brushing or repeat unchanged build/test milestones. Continue remaining Android/staging/device/store and disabled account-cleanup work without changing unrelated projects or approving licences for the owner.

**Policy checkpoint, 9 September 16:01 Toronto:** owner answered the retention question with five website policies. All five were fetched/read; use `PUBLISHED_POLICY_REVIEW.md` for source hashes, findings and native wording gaps. This does not approve a payment or backup retention period. Published policy text, Auth/DB and flags unchanged. The only new schema-path action was removing our own zero-byte, never-applied `20260909195513_mobile_account_identity_retirement.sql` placeholder; do not count it as migration24. Current source inventory remains 123 tables/23 completed migration fixtures. Supported Auth soft-retirement was researched but not implemented; provider grant evidence and final erasure remain unfinished. Continue safe independent work; do not invent retention or retry already accepted brush/build tests. Docker shutdown reply remains separate and pending.

**Newest checkpoint, 9 September 15:50 Toronto:** read `ACCOUNT_AUTOMATION_GUARD.md`. Main adds unapplied migration23 `20260909193514_mobile_account_automation_fence.sql` (SHA256 `456173c0a75fb661fb41d36d046c66748f01b40a027b754a8eb82dbce462ecf4`) and disabled Automation Worker guard/transport/tests/types. Permanent markers survive retries/redaction; server snapshot checks, search filtering, final webhook/email checks, safe queue retries and terminal-wait handling tested. Automation39/4, TS/lint/dry-run/types pass; all23 isolated SQL migrations/cases plus writer-first/fence-first and loaded-snapshot checks pass; Mobile968/55, both TS/contracts pass. No native edits/build, live deployment/push or network changes. Only Furnio fixture containers removed; do not stop LiveBy-Rep/Docker while shutdown question is unanswered. Final deletion hard-code false remains. Next: finish identity/session/provider and personal-data/retention cleanup, in-flight communications, SDK/picker cache ownership and real staging/store/device gates. Do not repeat accepted brushing or unchanged build/test milestones. No Mobile Git remote; Android's last exact SDK prerequisite check remains in LAPTOP_SETUP.md. Preserve all unrelated dirty work.

**Newest checkpoint, 9 September 15:21 Toronto:** read `ACCOUNT_PRIVACY_RATE_LIMITS.md`. Dedicated Mobile Worker now has source120/account10/receipt12 per-minute bindings, a new server-only HMAC key name/empty example, fail-closed configuration/timeout/errors, honest readiness and 429 Retry-After. Native transport has safe 429/503 wording and tests prove saved receipts survive/no automatic retry. **968 tests/55 files**, Mobile+Worker TS, frozen contracts, local Worker dry-run and123-table source inventory pass. No new SQL, native rebuild, deployment or push. Installed simulator build remains the previous accepted demo; new privacy wording not visually/staging-certified. Final deletion hard-code false and all flags false. Prior22-migration isolated SQL pass remains valid. No temporary Furnio container remains. Docker auto-started LiveBy-Rep earlier; owner was asked whether to quit it and has not answered—do not stop unrelated containers. Next substantive work: complete deletion's remaining identity/personal-data/automation/retention stages plus outstanding native/staging/device gates. Preserve unrelated work; do not redo owner-accepted brushing or completed unchanged builds/tests.

**Newest checkpoint, 9 September 15:04 Toronto:** read `NATIVE_SUBSCRIPTION_OVERLAPS.md`. Added read-only Admin potential-overlap warning/provider counts, reportVersion 2 and Action Centre text/tests. Main adds `20260909185306_native_subscription_overlap_reporting.sql`; Mobile adds SQL cases, wires the 22nd migration/test into existing harness, extends only synthetic fixture columns, updates baseline/preview. Admin102/16, TS/lint/build; Mobile923/54, TS/contracts pass. **Owner explicitly approved starting Docker/running isolated tests. All22 migrations plus overlap and existing financial/deletion/email races now PASS** after fixing a reserved-word CTE alias. Temporary test containers removed. **Docker auto-resumed pre-existing LiveBy-Rep Supabase with published ports; owner was informed and a second async question asks whether to quit Docker. Do not assume shutdown approval; check actual reply.** Offline new warning at1280/390/320 and exact-customer open/close/Next focus checks pass; file-based tab closed/no preview server. No native rebuild, deployment, live SQL or push; Mobile no remote and22 migrations unapplied to production. Do not repeat accepted brushing/completed builds. Continue substantive account/native/staging gates, respecting pending Docker choice and Android/account prerequisites. Preserve all prior dirty work.

**Newest checkpoint, 9 September 14:44 Toronto:** read `SUBMISSION_RECOVERY.md`. Studio now persists minimal native receipts before dispatch, prevents automatic retry after uncertainty, reconciles exact source IDs through the new read-only API and acknowledges only after the confirmed owned job renders. **923 Mobile tests / 54 files**, Mobile TS/contracts; **271 API tests / 24 files**, targeted lint/dry-run passed. The same four untouched reference-furniture API TS errors remain. New `MOBILE_SUBMISSION_RECOVERY_ENABLED=false`; no migration/deploy. Final beta Release/configuration log `output/ios-simulator-2026-09-09T18-41-41.796Z.log`, JS SHA256 `edbb43fc2d441c888e5d5e7063f64dbfac82643aeecbef7c63f82e90ede6d4a7`, installed with PID last 61325 (re-resolve). Native demo startup works; real Keychain restart/recovery UI and exact database projection remain staging acceptance, not passed. Next meaningful local work: remaining picker/SDK/batch cache ownership, support resolution for genuinely unresolved receipts, and larger account/billing gates. Do not repeat owner-accepted brushing or completed builds. Four Android dependencies still missing at 14:37; Mobile has no remote; Docker owner-stopped; 21 earlier migrations unapplied. Unrelated Main/Admin work preserved. No network settings or listeners changed.

**Newest checkpoint, 9 September 14:17 Toronto:** session-bound Studio/Batch uploads, sign-out-start cancellation, token refresh preservation, deadlines and late callback fencing are implemented. Read `UPLOAD_SESSION_SAFETY.md`. **892 tests / 53 files**, TypeScript and frozen contracts passed. Installed beta Release/configuration build: `output/ios-simulator-2026-09-09T18-13-01.094Z.log`; JS SHA256 `b345b311a313a00b4f5f4356b337b98f12143563b22d19aa104bab53fee075f6`. Native stock-photo Studio/result/Back and two-photo reviewed sample batch passed. App is on Home, PID last 56755; re-resolve containers/PIDs before use. No new crash in the bounded check. No build or extra listener remains. **Next: durable ordinary Studio submission receipts/reconciliation before retry after crash/reopen**, then picker/SDK cache ownership and dialog lifetime. Do not repeat owner-accepted brushing. Android's four exact packages remain missing (14:13), no Mobile remote, Docker owner-stopped, 21 migrations unapplied/all flags disabled. No production/shared-system changes.

Saved 9 September 2026, Toronto, before the owner's requested Mac restart.

**Resumed:** the owner has since returned, confirmed Chrome is working and explicitly asked to continue. The pause below is historical. Bounded offline Admin browser checks now pass; see the latest `PROGRESS.md` and `NATIVE_ADMIN_REPORTING.md` notes. The owner also confirmed the installed Xcode beta should be used for Furnio. Global build/network settings remain untouched.

## Pause and resume

**Newest checkpoint, 9 September 13:56 Toronto:** read `STUDIO_INPUT_NATIVE_QA.md`. Added nine-service preflight/request binding, reference append/remove/accessible pin controls and disabled-label validation UX. A real 13:31 glyph crash led to a tracked SVG main-thread measurement fix; a subsequent hidden-view failure led to a one-pixel preparation render. Final beta Release/configuration build `output/ios-simulator-2026-09-09T17-46-48.797Z.log` passed and installed hashes match (executable `1a4614c839f613453dc452fc8f3ca00de98b7f3ac57fd20af1757b2c321b02d1`, JS `25bf7b2b052aeae87cd5a122be9580d6c0c3aa86681ef9492e281fbd17616154`). Final native reference round trip, long/no-label full-resolution JPEGs, re-enabled invalid-colour rejection and cancelled-share cleanup passed. 846 tests / 50 files, TypeScript/contracts passed. App process 53840 was alive on Home; re-resolve PIDs/containers before use. No build/preview process remains. Do not re-test owner-accepted brushing. Next meaningful safety task: inspect ordinary Studio async upload/submit account-change/unmount fencing, then pending picker/batch cache ownership and native/staging gates. Four Android packages remain missing; no Mobile remote; Docker remains owner-stopped; 21 migrations unapplied/all flags disabled; no production changes. Known beta/dependency logs remain recorded rather than labelled clean.

**Newest checkpoint, 9 September 13:15 Toronto:** durable result-export ownership and restart cleanup are implemented and verified; read `EXPORT_RESTART_RECOVERY.md`. Source suite: 798 tests / 49 files, TypeScript and contracts passed. Beta Release/configuration build `output/ios-simulator-2026-09-09T17-11-12.106Z.log` passed, installed JS SHA256 `aba31f0d8988afced01fe2c9761fec6d14483031401afd00f7d887e8ee49e5d8`. Native normal-share cleanup, interrupted-share restart retention, injected expiry/partial-write recovery, and non-blocking unknown-file warning/retry passed. All current export fixtures were reclaimed; pre-existing picker/SDK files were deliberately not swept. Simulator is on Home; no build/server process remains. Next work includes remaining picker/SDK-before-return/batch-cache ownership, hidden invalid disclosure-setting UX, broader device/staging acceptance and the larger backend/account/billing gates. Do not redo accepted brushing or completed build checks. Android's four exact packages remain missing; Docker is owner-stopped; no Mobile remote/push or production changes.

**Newest checkpoint, 9 September 12:54 Toronto:** disclosure cancellation/readiness, stale-callback guards, measured text fitting, resolution checks and native reference release are implemented. Source suite: 745 tests / 47 files, TypeScript and contracts passed. Final beta Release/configuration build `output/ios-simulator-2026-09-09T16-49-34.710Z.log` is installed, with matching JS SHA256 `72be3c6dc2ee98547c7afc6e78550df2a22b9a20cb89118d91fbb39f8325734d`. Actual native 80-character wide-label and no-label JPEGs retained 4032 × 3024 resolution; clear over-limit validation/recovery and cancelled-share cleanup passed. See `DISCLOSURE_RENDER_HARDENING.md`. No build process remains running. Remaining meaningful work includes crash/restart owned-cache sanitation, hidden invalid disclosure-setting UX, broader native/device acceptance and the larger account/billing/staging gates. Do not repeat the owner's accepted brushing test or a completed build just because context compacted. Docker remains owner-stopped; Android's four packages remain missing; no Mobile remote or push.

**Newest checkpoint, 9 September 12:33 Toronto:** native local JPEG/disclosure testing is implemented and installed (`output/ios-simulator-2026-09-09T16-23-56.015Z.log`). Source suite: 696 tests / 46 files, TypeScript and contracts passed. Two real 4032 × 3024 photos were saved with/without disclosure; alternate font/position, native share opening/cancellation and export-file lifetime/cleanup passed. No external sharing, paid calls or production changes. See `NATIVE_JPEG_EXPORT_QA.md`. The installed JS matches `a5f07b44a404a38772bfdd95fc204982afea5400a89a0cc43a36b6f214942823`. Do not rerun completed native build/brush acceptance merely after compaction. Next meaningful local work: harden disclosure capture error/timeout/unmount paths and long-text fitting, then continue remaining native/device/backend gates. Android's exact four SDK packages still missing at 12:33; Docker remains owner-stopped. Mobile still has no remote and these changes are not pushed.

**Newer checkpoint, 9 September 12:15 Toronto:** the native Batch review fixes are implemented, tested and installed (`output/ios-simulator-2026-09-09T16-11-06.056Z.log`). Source suite: 687 tests / 45 files, TypeScript and contracts passed. Two-photo Item removal QA verified real PNG preparation, independent review gates, point persistence, replacement cleanup, sample-only submission and visible Back. Test-created batch masks are now all cleaned; simulator is on Create. See `BATCH_MASK_REVIEW.md`. No build/preview listener was left running. Android's same four exact packages were rechecked and remain missing; Docker still needs permission before restart. Full-resolution JPEG/disclosure and sharing are next distinct checks, not yet passed.

**Previous checkpoint, 9 September about 12:03 Toronto:** native mask review and exact PNG-export validation were implemented locally. The corrected beta Release build `output/ios-simulator-2026-09-09T15-56-28.459Z.log` was installed in Furnio Mobile QA. Limited native checks passed for visible 2048×1536 mask pixels, alignment, two separate regions, review close/reopen preservation and Undo. Source checks: 672 tests / 43 files, TypeScript and frozen contracts passed. See `NATIVE_MASK_REVIEW.md` for the pinned iOS SVG patch, stale-Pods-path build guard and missing upstream artifact/cached Release dependency caveat. Do not restart a completed build or rerun the owner's already-passed directional brushing test.

The prior email milestone is unchanged: `ACCOUNT_EMAIL_GUARD.md` records a disabled email-consumer safeguard and the twenty-first unapplied mobile migration. Email Worker: 33 tests, TypeScript, targeted lint and local dry-run build passed. The new SQL and concurrent-dispatch tests are authored but unexecuted; Docker remains stopped and needs the owner's go-ahead before restarting. Source inventory is 123 tables. Automation suppression and complete deletion remain unfinished. No production changes occurred.

The noon build's native executable matched SHA256 `5deb6d9504d0e0ec5dfc0d2ed47f3fc5d6350f4b00fd8157d290c16a5314cd17`. The newer batch build's installed JS matches its output SHA256 `fbee7a258968573c52c622439421a645b01cfd8343822de8aace834265c0891e`. **The owner confirmed brushing works perfectly in all directions** on the preceding gesture-fix build; those controls were retained. See `IOS_BETA_QA.md` for historical and latest evidence. No Metro server or additional listener was started, and no global Xcode/network settings were changed.

The owner asked to save the work, restart the Mac and resume after returning. Do not restart the computer, Docker, browser sessions, development servers or builds on their behalf. Do not change DNS, proxy, firewall or TCP settings. Wait for the owner to return before resuming implementation/testing.

The full approved mobile plan remains in progress, not completed or ready for customers. Read `PROGRESS.md`, `IMPLEMENTATION_PLAN.md` if present, `API_CONTRACT.md` and the relevant milestone documents before continuing; inspect the actual repository state rather than assuming the earlier environment survived the restart.

## Saved source

- Mobile: `/Users/alipashaamidi/Dev/Furnio Mobile`, branch `codex/mobile-foundation`. Initial local checkpoint `43773f5` saved this handoff and the source. No Git remote is configured and nothing was pushed. Subsequent local edits are listed by `git status`.
- Main: `/Users/alipashaamidi/Dev/AI Virtual Staging`, origin `https://github.com/pashatech25/furnio-main.git`, existing HEAD `19908507ecb032abaa38b07df49648e677dd880c`. Mobile compatibility source and twenty-one new migration files are saved on disk but remain uncommitted alongside unrelated reference-furniture/upload work. Do not reset, stash, commit wholesale or discard those edits.
- Admin: `/Users/alipashaamidi/Dev/Furnio Admin`, origin `https://github.com/pashatech25/furnio-admin.git`, existing HEAD `f9d6e913532ce36f75ddc6c627ab0b0f8eb84e65`. Native customer/reporting compatibility source is saved on disk but remains uncommitted. Preserve the unrelated `.codex-tmp/` directory.
- The Mobile initial candidate set was checked for common private-key/token signatures and credential-like filenames; no matches were found. Environment examples contain empty credential placeholders and disabled gates. This narrow check is not a comprehensive security audit.
- Generated native projects, dependencies, logs and screenshots are ignored by Git and remain on disk. A local commit is not an off-machine backup.

No production migration, push, Worker deployment, paid image request, real purchase or store submission was performed during this save operation. All native rollout flags remain disabled.

## Last verified implementation state

- Latest feature: separately gated native Admin reporting, with provider/currency-separated exact amounts, recorded subscription states and paginated follow-up; see `NATIVE_ADMIN_REPORTING.md`.
- Last completed checks before the connectivity interruption: 625 Mobile/backend tests across 38 files; 82 Admin tests across 14 files; Mobile/Admin TypeScript; frozen contracts; targeted Admin lint/build; isolated SQL/concurrency tests for twenty unapplied migrations. The account inventory covers 122 tables. These tests were not rerun during checkpoint saving.
- Latest installed iOS simulator Release build: `output/ios-simulator-2026-09-09T12-57-11.958Z.log`. The demo welcome screen was inspected; physical-device, authenticated staging and store testing remain open.
- Dedicated simulator: Furnio Mobile QA, UUID `890062D4-AB26-4389-94E5-043C1C95999D`, bundle `ai.furnio.app`. Recheck its state after restarting.
- The new Admin reporting panel's desktop/mobile visual and interaction acceptance is still pending. Its generated offline fixture is not proof of passing browser QA.
- No own test/build process was intentionally left running. The temporary preview server on port 4387 and the task's named Playwright browser were closed. Pre-existing port 4381 and unrelated browser/dev processes were not stopped by this task.

## Connection interruption findings

The owner reported Chrome `ERR_ADDRESS_INVALID` even after switching to a phone hotspot; Firefox still worked. Read-only checks around 09:46–09:49 found:

- The hotspot's IPv6 path could load `https://realviu.com/` successfully (HTTP 200 after redirects).
- Default IPv4 connection attempts failed immediately with “Can't assign requested address.”
- The same IPv4 request succeeded (HTTP 200) when this one diagnostic request used a free source port in 41011–41015. This did not create a listening server or change system settings.
- Approximately 16,000 lingering IPv4 `TIME_WAIT` sockets still referenced the old Wi-Fi address. These are closed-connection records, not 16,000 listening servers.
- Nothing was listening on TCP port 16000 in the check. This is a current snapshot, not proof of historical ownership.
- Evidence strongly points to temporary local IPv4 port exhaustion. It does not establish why the buildup occurred or prove an attack. Earlier process counts included Docker, Codex, Firefox and Chrome; attribution to a process cannot identify which particular Codex conversation caused traffic.
- The owner stopped Docker. Do not restart it automatically. Do not change system TCP limits or clear Chrome profiles/passwords as a workaround.

After the owner returns, first ask whether Chrome works after restart; use only bounded checks if further diagnosis is needed. Do not resume repeated browser/network testing while the connection is still failing.

## What the laptop still needs

The latest directory checks found these four exact Android prerequisites absent under `/Users/alipashaamidi/Library/Android/sdk`:

1. `platforms/android-36` (Android API 36.0, not only 36.1).
2. `build-tools/36.0.0`.
3. `cmdline-tools/latest`.
4. `ndk/27.1.12297006`.

See `LAPTOP_SETUP.md` for the Android Studio click-by-click instructions. Recheck before requesting installation again. Xcode/iOS runtime, Java, CocoaPods, Node, pnpm and Android Studio are already installed.

Separate account/configuration requirements remain: isolated staging, a new Mobile Git remote, Apple Developer/App Store Connect, Google Play, RevenueCat, Expo where needed, approved product prices/agreements/privacy and physical-device acceptance. Never reuse production or unrelated Docker projects to bypass these gates.

## Remaining scope — do not declare completion

Complete shared-account deletion and provider/session revocation; native Action Centre and automation compatibility; unresolved purchase/reconciliation cases; real staging authentication/SMS/all-nine-service prompt parity and regression tests; Android binaries; real sandbox commerce; push credentials/device testing; privacy/signing/private builds; and owner-approved store release remain. `PROGRESS.md` records the detailed boundaries. Native purchases and final account deletion must stay disabled until their full release gates pass.
# Usage-budget pause — 9 September, 17:45 Toronto

**Superseded by owner's “no just continue as you had planned, it's ok”.** Continue the release plan. Follow-up completed the test lint fixes and decimal-filter compatibility; **72 Worker tests**, TypeScript/lint/types-check/dry-run and the **24-migration SQL suite pass**. New migration hash is reviewed in `account-schema-baseline.json`; see `NATIVE_AUTOMATION_BILLING.md`. The pending lint/hash statements below are historical. Staging configuration is the next external dependency; see `STAGING_SETUP.md`. No production change, push or native rebuild.

Owner reports about 13% weekly usage remaining and asks how far submission is. Pause additional implementation/build loops and explain the release gap before resuming.

- Android prerequisite/build/native QA milestone is complete; see the 17:26 checkpoint in `ANDROID_NATIVE_QA.md` and `PROGRESS.md`. Do not repeat unchanged builds.
- Read-only local Supabase CLI inventory found `Furnio-Production` (`sgsjkgfwgxmlqcgyuyeh`) and no preview branches. The Supabase MCP account differs and cannot access Furnio. An async question asks whether separate Furnio staging exists under another account. Do not reuse another project or create paid resources without confirming the target/cost.
- New local, unapplied migration `20260909213457_native_automation_customer_billing.sql` adds a bounded service-only, environment-checked customer reader. Find/Select use it only with `NATIVE_AUTOMATION_BILLING_ENABLED=true`; default is false. Legacy Stripe columns remain untouched; additive `billing` includes native states. An empty/error response never falls back to a Stripe-only unsubscribed audience. Existing account-deletion filtering remains.
- Current native automation slice: **71 Worker tests / 6 files and TypeScript passed**. **24 migrations and SQL/concurrency fixture suite passed**, including new automation billing/permissions/environment cases. Fixture container removed; no live data, external messages or paid calls.
- NOT complete: targeted lint has **11 test-only diagnostics** in `native-customers.test.ts` and `native-customer-engine.test.ts` (type imports, unused destructuring variables, async without await, stringification). Final dry-run/types check did not execute because lint stopped the chain. Migration hash has NOT yet been added to the reviewed account-schema baseline. Document the filter history semantics and decide whether to expose additive billing conditions in Admin before enabling. New source remains uncommitted; no deploy/push.
- No production changes, new development servers, network changes, native rebuild or unrelated Docker shutdown in this slice. Mobile still has no Git remote.
- Submission still requires isolated staging integration, real sandbox commerce/signing/accounts, complete deletion + approved retention/privacy, push credentials, physical-device/private testing and store packaging/owner acceptance. This is a working native prototype with substantial local backend work, not a store-ready release.
