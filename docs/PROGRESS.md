# Furnio Mobile — implementation status

## Current checkpoint — 9 September evening, real authentication integration

- **FAL key connected after the owner created it.** Copied/saved securely, installed only in the four staging Workers; no duplicate key or production key change. **23 hosted preflight checks pass** for authentication, credit confirmation, malformed service inputs, callback-signature rejection and blocked Stripe checkout, with job count/balance unchanged. No paid FAL call yet. Awaiting the owner's requested **up-to-US$5 test-budget confirmation**. See `STAGING_PROCESSING.md`; prior “key pending” notes below are superseded.
- **22:38 Toronto: real private storage/upload passes; staging processors deployed.** See `STAGING_PROCESSING.md` for exact evidence and deployment. Hosted JPEG upload, image validation/watermark preview and account-isolation checks passed twice, without FAL calls. Three isolated Workers reuse existing media/floor-plan/reference-furniture implementations. All 9 enabled services, 15 Admin prompts, model settings, pipeline and trial policy now match production's configuration-only snapshot. Production is read-only/unchanged. Earlier staging trial policy was disabled; now enabled to match Admin, but previously verified test accounts were not silently re-granted trials. Generation remains gated pending a separate FAL key and explicit paid-test limit. 151 focused API tests, targeted lint and diff check passed; native upload/generation and physical-device acceptance remain unpassed.
- **22:05 Toronto: Google account switching fixed, installed and visually verified.** Native OAuth now sends `prompt=select_account` on every Google sign-in, preserving PKCE/callback validation and leaving Google's shared browser session alone. Ten focused auth tests and TypeScript pass. Release `output/ios-simulator-2026-09-10T02-03-53.904Z.log` built/installed. Tested app-only sign-out → Google → Continue: screenshot `output/google-account-chooser.png` visibly shows **Choose an account** and **Use another account**, with no automatic return to the prior account. Left this chooser open for the owner. No production OAuth/client changes.
- **Phone completion and duplicate guard verified against deployed staging.** Owner reports completing verification; read-only SQL confirms their confirmed Auth phone, active Furnio phone claim, valid unique phone index, and both reservation/claim checks. A second synthetic staging identity calling the deployed `reserve_phone_verification` with the already-claimed phone was rejected with the expected duplicate-account message. The entire diagnostic ran in a read-only transaction: no SMS, data changes or production access. A second real Google account's full native rejection screen is still not observed. Earlier OTP-pending notes below are historical.
- **Not complete and not submitted.** Real uploads/processing across all nine services, maps, remaining authentication/account-deletion/push acceptance, physical iPhone/Android end-to-end tests, production rollout, signed builds/store materials and review submission remain. Local tests/native screens are not evidence of completed real generation. Play's recorded closed-testing gate also remains. Website-only purchase decision is unchanged.

- **21:55 Toronto: owner-reported code-entry UX fixed and installed.** Read the website's `trial-onboarding-dialog.tsx`: it uses mutually exclusive phone/code steps. Native now follows that flow; the phone input disappears after sending, code/Verify are first, and change-number/resend remain secondary. Server-reported pending `new_phone` restores the owner's code step after reinstall without sending another text or bypassing OTP. Release `output/ios-simulator-2026-09-10T01-54-03.373Z.log` installed; actual AX and screenshot `output/staging-code-step-corrected.png` show **Enter your code**, six-digit input and no phone-entry form. 13 focused native tests and typecheck/build pass. Owner received SMS; completed OTP/dashboard still pending.
- **21:51 Toronto: actual staging phone-start succeeded.** Separate Turnstile widget `0x4AAAAAAEue1jhZKP6PJAEE` created and its credential/domain validation passed. Staging Supabase CAPTCHA enabled; production Auth re-read unchanged. Customer staging Worker `52c56c37-b8b3-4413-8d80-8ed9501ba1ef` and support staging Worker `dd889bd7-768d-43c1-acb5-dcdeab18a315` deployed successfully. Native WebView now permits Turnstile's required storage and owned blank/srcdoc child frames while blocking unrelated/top-level navigation. Release `output/ios-simulator-2026-09-10T01-42-54.772Z.log` installed; screenshot inspected and displays **Success!**. Existing Twilio Verify credential authenticated and connected only to staging, matching the website's current RealViu subaccount/service (no Twilio service changes). One SMS requested through the actual native/customer/Siteverify/Supabase/Twilio path; app displays **We sent a code** and the OTP input. Owner is entering OTP; do not mark completed verification/dashboard/trial acceptance until observed. See `STAGING_SECURITY.md`. 11 native auth/navigation tests, 36 customer gate/trial tests, 22 support endpoint tests and native typecheck/build pass. Paid/image endpoints remain closed.
- **Security credentials approved and connected, superseding the pending notes below.** The approved external Wrangler 4.129.0 authenticated successfully. Spin's helper rejected the valid current OAuth bearer format locally; only its token-format check was adapted in memory to the standard bearer character set, retaining its scope and validator logic. Permission probe and widget validator passed. No credential appeared in logs/argv/app bundles. Private widget credential resides only in the ignored owner-only staging env, staging Worker secret and staging Supabase Auth. Phone pepper is a separate random staging Worker secret. No production widget/secret/Worker deployment.
- **Do not ask owner to retry the blocked verification screen.** The native challenge endpoint still has empty Turnstile site key/hostname; SMS provider remains disabled. Replacement simulator build `output/ios-simulator-2026-09-10T01-30-00.407Z.log` installed and its actual screen inspected: identifies incomplete service configuration instead of blaming internet or presenting a futile retry. HTTP service failures/network failures have separate messages; failed notices use natural height. Mobile TypeScript/30 focused tests pass. Main's staging-only gate now permits the three existing phone handlers only when the exact staging hostname, site key, secret and phone pepper are configured; 36 gate/trial tests pass. This source change is **not deployed**, cannot enable anything with current missing secrets, and does not affect the production entrypoint.
- Earlier checkpoint: Cloudflare dashboard login verified and external Wrangler approval requested. **Resolved by the 21:51 checkpoint above.** Never use project-local Wrangler for Turnstile credential operations.
- **Owner Google sign-in succeeded; post-login crash fixed and simulator-verified.** Crash report `Furnio-2026-09-09-211620.ips` and native JS log identify `TypeError: undefined cannot be used as a constructor` in `Verify`: Hermes lacks `Intl.DisplayNames`. Replaced it with bundled English country labels covering every selectable calling-code country; explicit missing-Intl regression test added. TypeScript and 35 focused tests pass. Replacement Release build `output/ios-simulator-2026-09-10T01-18-56.719Z.log` installed successfully. Owner's saved Google session restored to phone verification; Canada +1 displays and opening/selecting Canada in the country list passes without crashing. SMS button remains disabled because Turnstile/Twilio setup is still pending. No verification bypass or account reset.
- **Not release-ready.** Owner requested real registration, verification, service processing and a physical iPhone test; those must precede submission claims.
- Google: added only `https://bcrobmrimzkvfrnqarmv.supabase.co/auth/v1/callback` to the existing FreeHomeValue OAuth client. All 15 origins, four previous redirects and both enabled secrets preserved. An existing downloaded, Google-validated credential is configured in staging. Owner completed Google sign-in; callback, secure session persistence and cold-start restoration to the pending-phone screen verified. Production must use the existing `auth.furnio.ai` custom domain; the visible raw Supabase address is staging only.
- Staging email: connected the existing Furnio SMTP sender using its saved credential, verified with TLS SMTP AUTH (235). No confirmation email has yet been sent/tested. Supabase configuration reads mask secrets; masked fingerprints must never be copied as credentials. Earlier unusable staging SMTP values were removed and replaced with the verified raw credential. Production Auth was independently re-read unchanged.
- Staging native callbacks and native Apple bundle audience configured; real Apple login still pending. **Staging SMS is now connected with the verified raw Twilio credential**, six/hour rate limit, 60-second resend and required OTP confirmation; the earlier unusable fingerprint was replaced. One actual phone-start request succeeded; owner OTP entry pending.
- Email signup UI now has required terms acknowledgement, optional marketing, CAPTCHA token plumbing, an inbox confirmation/resend screen and password-reset mode. Public-settings connection failures have an explicit retry. Authentication/verification are not considered passed from fixture accounts.
- Address autocomplete and the same style of static property map as the website are implemented. The website Mapbox token returned 403 for native requests and 200 with the legitimate website origin in a diagnostic request, confirming URL restrictions. **Do not remove those restrictions or ship a spoofed website Referer.** A separate public native token is awaiting owner Mapbox sign-in; native map acceptance remains pending.
- iOS staging Release rebuilt successfully: `output/ios-simulator-2026-09-10T01-09-26.718Z.log`, installed on the dedicated simulator. Latest connection-retry change still needs inclusion in the next build. No physical iPhone detected by devicectl; no TestFlight upload or submission.
- Targeted signup/address/challenge tests and TypeScript passed before this checkpoint; build-environment tests pass with secret-token rejection. Support Worker challenge-action changes are local, **not deployed yet**. Customer staging still blocks SMS, uploads and generation. No real imaging, SMS, production deployment or production database mutation performed.
- Credential-bearing setup is pinned to staging in `scripts/staging-auth-config.mjs`; downloaded credentials never enter Expo, source or logs. Mobile repo still has no remote. Preserve all unrelated changes.

## Previous checkpoint — both Apple signing identities verified

Owner's final Xcode screenshot shows separate Apple Development and Apple Distribution certificates. Local `security find-identity -v -p codesigning` confirms **2 valid identities**, and Distribution subject/OU matches team `5SY24C9RBH`. Signing certificate setup is complete; no duplicate types or additional creation needed. See `IOS_SIGNING.md`. Earlier window-control/pending Distribution notes below are superseded. No new profile/archive/upload, app code change, deployment or production mutation in this verification.

## Previous checkpoint — Apple registration and Android authentication acceptance

- Apple membership/agreement resolved. Registered explicit `ai.furnio.app` with Push Notifications and Sign In with Apple; created Furnio ASC record **6810452958** (`furnio-ios-001`), iOS/English (U.S.), **1.0 Prepare for Submission**, manual release. Subtitle/categories saved. No binary uploaded/submitted/published.
- Owner Xcode login verified: team **5SY24C9RBH**, Admin; owner explicitly approved both signing certificates. Apple Development created and verified as **one valid local signing identity** after adding Apple's verified public WWDR G3 intermediate with default trust. No private-key export, trust override or certificate revocation. Distribution creation interrupted by Xcode window-control failure and remains pending; owner asked to bring Xcode beta forward. See `IOS_SIGNING.md`. Public team ID persisted in app.config.ts; TypeScript/four commerce-policy tests passed. Generated project/team and release-version alignment remain pending.
- Android staging wallet/project/cold-start/sign-out/account-switch checks passed on the existing APK. Second synthetic account has its own 20-credit balance and no projects; previous owner's project is absent. Screenshot evidence in `ANDROID_NATIVE_QA.md`. The earlier iPhone checks remain passed, not repeated.
- Apple released Xcode 27 RC today; existing beta 6 remains unchanged for QA, and RC/store-toolchain verification is pending. No new app build, production modification, migration, Worker deployment or push in this checkpoint.
- Still open: certificate/profile/store packaging, real OAuth/SMS, isolated imaging/all-nine-service acceptance, complete deletion/retention, push, production-compatible rollout and physical/private testing. Play's 12-tester/14-day gate remains. See `STORE_SUBMISSION_STATUS.md`; do not call the app release-ready.

## Previous checkpoint — 9 September, website wallet and native builds

- RevenueCat removed from native autolinking/plugin/runtime dependencies (deferred tests retain a dev dependency); CocoaPods removed all three purchase pods. iOS binary/JS and Android DEX inspected without store SDK markers.
- Wallet uses authenticated existing customer GET `/api/billing`, with real balance/subscription/recent credit-purchase data and no native-payment SQL dependency. The home button now says “View credits.” Existing credit rules and production website remain unchanged.
- **1,069 tests / 60 files**, TypeScript, frozen contracts and diff checks pass. Main's isolated staging gate passes **28 tests**. Hosted staging passes **10 grouped checks**; checkout/messages/generation blocked, no spend or purchases.
- Only the customer staging Worker was deployed: `a04e1d98-7e05-4a38-9e19-6258bf50e892`. No production deployment, migration or push. No unchanged imaging Worker deployed.
- Native iOS/Android staging Release builds pass. iOS actual synthetic sign-in, 20-credit wallet, project detail, cold-start session recovery, sign-out and account switching pass. Ad-hoc simulator signing resolved the old unsigned-build Keychain error without changing storage/security logic. Android installation and synthetic owner sign-in pass: correct20creditbalance/project/View credits label verified in `output/android-staging-login-result.png`; wallet/session/account-switching checks remain.
- Google Play Furnio draft created by owner in verified account; **12 testers / 14 continuous days** required before applying for production. Apple membership now verified active (Individual, team `5SY24C9RBH`), ASC New App form accessible; Bundle ID/signing/app record not created. Agreement banner discrepancy remains to reconcile. No store binary uploaded or release submitted. See `STORE_SUBMISSION_STATUS.md`.
- Latest artifacts: iOS log `output/ios-simulator-2026-09-09T23-46-42.485Z.log`; Android log `output/android-emulator-2026-09-09T23-46-43.645Z.log`, APK SHA256 `7116bc6c3821b03ded45a449acf0dba4ace4636c0b1c48d61ffc3fc2ca9ed504`. These are QA builds, not store-signed releases.

Remaining: Apple identifier/signing/app record and agreement-banner check, real OAuth/SMS, isolated imaging and all-nine-service acceptance, complete deletion/retention, push, minimal production-compatible rollout and physical/private testing. No release-ready claim.

## Current owner decision — website-only purchases, 9 September

**Supersedes native purchase requirements below.** Owner approved a free companion app with purchases only on Furnio.ai. RevenueCat/store setup is stopped; no native purchases are live. Read `WEB_PURCHASES_DECISION.md` before continuing. Wallet is now read-only with account/foreground refresh and neutral zero-credit state; store calls also have a non-environment disabled guard. Preserve historical commerce code/migrations, but do not deploy them wholesale or change existing website credit rules. Production has not received the mobile migrations; the 59 applied migrations are in isolated staging only. Device/store/privacy/real-service acceptance remains; this decision does not mean ready to submit.

Verification of the companion revision: **1,057 tests / 59 files**, Mobile TypeScript, frozen customer contracts and `git diff --check` pass. No new native build/visual acceptance is claimed. See the decision document for remaining SDK/generated-project cleanup and production read-only wallet adapter simplification. No Main/Admin/Worker/SQL/deployment/push was performed in this revision.

## Previous checkpoint — hosted staging, 9 September 19:05 Toronto

**Supersedes all earlier staging/build holds below.** Staging has 59 reviewed migrations (legacy Stripe data cleanup skipped), all 107 public tables under RLS, three synthetic accounts and two isolated deployed Workers. The 19 hosted SQL checks and nine grouped authenticated API checks pass, including pending-phone protection, cross-account projects/RLS, real balance/activity and blocked paid/outbound routes. Two workerd transport bugs were found/fixed; 1,054 Mobile tests, Mobile/Worker TS and 179 targeted Main tests pass. No production migration/deploy/push or paid test. See `STAGING_INTEGRATION.md` for exact versions and commands. Public-only staging native builds are now being verified. RevenueCat owner signed in and authorized remaining setup; store sign-in/agreements, products, real imaging/SMS/OAuth/push, full deletion/retention and physical-device/private-store acceptance are still open. Do not recreate staging or repeat laptop installation.

## Historical checkpoints (not current instructions)

**Staging created, 9 September:** `Furnio-Mobile-Staging` (`bcrobmrimzkvfrnqarmv`) is healthy in Furnio's existing Pro organization/us-east-1, Micro, quoted additional $10/month. Dashboard SELECT verifies zero Auth users/Storage objects/public tables. Password is generated, ignored and mode0600; no keys exposed or customer data copied. Production CLI link remains unchanged. No schema migrations, Worker deployments, native app reconnection, purchases or paid AI tests occurred. Next is staging schema/test configuration; see `STAGING_SETUP.md` for the TLS CA connection follow-up. Earlier approval/sign-in holds are resolved; do not repeat them.

**Staging approval received, 9 September:** owner explicitly authorized creating a separate Supabase staging project. Fresh CLI inventory confirms the intended Furnio organization and no existing Furnio staging project. Browser requires owner sign-in to check the organization billing plan before provisioning. The sign-in page is open; project has NOT yet been created. See `STAGING_SETUP.md`. Older statements that staging approval is unanswered are superseded; other store/retention/testing gates remain. No production changes, deployments, copied customer data or new local servers.

**18:12 Toronto dependency checkpoint:** the four RN0.86.3 upstream archives now return200 and match the existing SHA256s. Refreshed only React-Core-prebuilt/ReactNativeDependencies metadata without test-only overrides; all pod versions are unchanged. Local Release build/embedded config, framework signatures and exact-build simulator startup pass. New local build-helper guards reject stale local-file metadata. Read `IOS_DEPENDENCY_READINESS.md` for the four beta-Xcode diagnostics despite exit0, known warnings and clean-CI/store limits. Latest iOS build `output/ios-simulator-2026-09-09T22-08-17.383Z.log`, JS `e45a26944c3e40c80f9193205aeb9ebbe4c18f2bbe55186f664f747f4676beb4`; dedicated QA simulator left on sample welcome, PID18181 at check. No new laptop install, server/port, production/SQL/Worker change, paid resource, Git push or database action in this milestone. **Staging approval and store/retention/physical-device gates remain pending.**

**Latest full-schema checkpoint, 9 September:** `pnpm test:schema` replays all **60 Main/Admin migrations** (including **25 mobile migrations**) and passes **19 real-application-schema checks**. It caught and fixed a native purchase rollback for trials awaiting phone verification. The additive date-constraint exception leaves verification required and the shared trial-conversion function unchanged. See `FULL_SCHEMA_REPLAY.md` for the failure, exact fix, tests and Auth/Storage stand-in limitations. Source inventory remains 123 tables. Only owned RAM-only, port-free fixtures were removed. No hosted migration/deployment, production change, native rebuild or push. **Next requires the pending staging approval in `STAGING_SETUP.md`; do not provision paid resources without it.** The earlier minimal-ledger suite covers 24 migrations; the new full-schema suite validates the 25th.

**17:47 Toronto continuation:** owner asked to continue the release plan after the usage-budget discussion. Native-aware Automation Find/Select reader is now locally verified: **72 Worker tests / 6 files**, TS/lint/generated types/dry-run pass; **24 mobile migrations** and SQL/concurrency cases pass in the isolated fixture. No hosted migration/deployment. Reviewed source inventory remains 123 tables. Read `NATIVE_AUTOMATION_BILLING.md` for exact history/status semantics and remaining Stripe-only Condition/queued-snapshot limits. `STAGING_SETUP.md` records that the local CLI finds Furnio production but no Furnio staging/branches; owner choice/paid-project approval remains needed. No repeated native builds, production changes, new server or unrelated Docker action.

**17:26 Toronto Android export checkpoint:** actual Android QA found and fixed the data-URI mask decoder failure and SVG cached-image/text-readiness failures. Extended the existing pinned SVG patch only for Android; iOS native hunks/decoder remain unchanged. Added honest system font equivalents and stable accessible button titles. Two distinct mask PNGs/review/Undo passed on the mask-fix build; final APK passed actual960×640 serif JPEG save, same-source monospace export/native share/cancellation, and no-label JPEG save. Final **1,043 tests / 58 files**, TS/contracts, build/package/bundle/signature/ZIP alignment pass. Log `output/android-emulator-2026-09-09T21-20-36.780Z.log`; APK `10e394e8f3bc484633155034cecea7b9e7d375780fb77f9a3fbe09c62982722e`. Read `ANDROID_NATIVE_QA.md` for intermediate failures/evidence/limits. This closes the current laptop/Android local QA milestone, not staging/store/physical-device/account-deletion acceptance. No Main/Admin/Worker/SQL/production/Docker/network change or push; no Mobile remote. Emulator left on sample Home, PID last9961; no Metro/build process remains. Android private-cache inspection, large-photo/stress/16KB-runtime, real services/commerce and retention gates remain. All rollout flags stay disabled.

**16:54 Toronto Android checkpoint:** the owner installed Android prerequisites; added the missing base API36 platform alongside its extension platform. The real ARM64 Release/demo build now passes, installs and runs on existing `Pixel_9_Pro_XL_2` (Android16/API36, temporary read-only session). Native system photo picker, owned sample-photo import, Modern style, in-app confirmation and simulated processing-to-result flow passed. Fixed Create icon/label overlap and bottom gesture spacing **only on Android**, rebuilt and visually verified the final Home/tab bar. Final APK SHA256 `59b4610a65ff29ba98f2fa393929be47fe8de296082dae18d29e1d734a775c87`; log `output/android-emulator-2026-09-09T20-48-11.358Z.log`; see `ANDROID_NATIVE_QA.md`. TypeScript/package/bundle/signature/16KB ZIP alignment checks pass; dependency warnings remain and this emulator uses 4KB runtime pages. No more software is needed for this laptop Android milestone. This does not certify physical devices, store signing, native purchases or real services. No Main/Admin/Worker/SQL/production/Docker/network changes or push; no Mobile remote. Emulator is left open on sample Home, no Metro server. All rollout gates remain disabled; continue staging/account/store/deletion and remaining device acceptance, not already-accepted brush/build tests.

**16:31 Toronto update:** completed owned native photo-input sessions, returned-SDK-file journalling, late-callback cleanup and restart recovery across Studio/Batch/result-demo pickers. Real simulator selection exposed an asynchronous `File.copy` bug hidden by synchronous mocks; inputs, saved drafts and final JPEG export now await copies, with delayed-copy regression tests. **1,024 tests / 57 files**, Mobile TS/contracts and final beta Release/configuration build pass. Native stock-photo selection, editor-exit cleanup, inspected 1668 × 2500 labelled JPEG, cancelled-share cleanup and restart recovery pass on the final installed build. Historical SDK files/originals remain untouched. See `PHOTO_INPUT_PRIVACY.md` for exact build/hash, intermediate failure and remaining boundaries. No web/Admin/Worker/SQL/production/network/Docker change. **Android inventory now shows only `platforms/android-36` missing**; Build-Tools36.0.0, latest command-line tools and NDK27.1.12297006 are installed by directory checks. No Mobile remote; flags remain disabled. Continue Android once that platform is installed and remaining staging/store/retention work; do not repeat owner-accepted brushing or unchanged checks.

**16:01 Toronto policy checkpoint:** the owner supplied all five published policy URLs. Read and recorded their commitments, exact content hashes, unanswered retention periods and required native-commerce/privacy wording in `PUBLISHED_POLICY_REVIEW.md`. Image retention is intended for 30 days; financial/backup periods are not defined by those pages. No published policy, production setting, Auth identity, application logic or deletion flag was changed. Removed only the empty, never-applied identity-retirement migration placeholder; the last completed database milestone remains **23** migrations. Independent development can continue, but full deletion and store release still require the retention schedule and remaining implementation/acceptance gates.

**15:50 Toronto update:** implemented the disabled automation account-deletion checkpoint; see `ACCOUNT_AUTOMATION_GUARD.md`. Permanent event/run/wait/delivery suppression, server-only exact-snapshot checks, bounded/redacted Worker transport, new-search filtering and final action rechecks are in place. Terminal waits cannot restart work. **39 Automation Worker tests / four files**, TS/lint/local dry-run/types check pass; **all 23 mobile migrations pass isolated SQL**, including new worker-role and two-connection writer/fence cases. **968 Mobile/backend tests / 55 files**, Mobile/Worker TS and contracts pass. Inventory remains 123 tables with the new migration hash reviewed. No deployment, push, native rebuild, paid processing, live message or network change. Final deletion remains code-blocked and the new flag is false; in-flight delivery, full erasure/identity/retention and staging/device/store gates remain. Temporary fixture removed; unrelated Docker stack untouched pending the owner's shutdown reply.

**15:21 Toronto update:** added independent account-privacy rate limits and recovery-safe native 429/503 messages. See `ACCOUNT_PRIVACY_RATE_LIMITS.md`. Source/account/full-receipt keys are HMAC-pseudonymous, missing/failed/hung protection stops before database work, and status remains independent of sign-in/entry flags. **968 tests / 55 files**, Mobile/Worker TS, contracts, local Worker dry-run and source inventory pass. No new SQL; the earlier owner-authorized 22-migration isolated pass remains current. No deployment/push/native rebuild; installed simulator demo predates the new message copy. Final deletion is still code-blocked/all flags false; real edge/native/cleanup/retention gates remain. Docker's unrelated auto-resumed stack is untouched while its shutdown question awaits a reply. Brushing remains owner-accepted.

**15:04 Toronto update:** implemented read-only Admin subscription-overlap follow-up; see `NATIVE_SUBSCRIPTION_OVERLAPS.md`. Distinct native families and relevant customer Stripe records produce one potential-overlap signal per customer; unknown Stripe mode is explicit. No checkout/credit/cancellation/tax/revenue logic changed. **102 Admin tests / 16 files**, TS/lint/build; **923 Mobile tests / 54 files**, TS/contracts pass. Owner approved Docker: all **22 mobile migrations**, new overlap cases and existing ledger/purchase/deletion/email concurrency tests pass in the isolated fixture after correcting a SQL CTE reserved name. New warning file-based desktop/mobile visual, exact-customer focus, close/Next and 320px layout checks pass. No native rebuild, live deployment or push. Test containers removed; Docker auto-resumed pre-existing LiveBy-Rep containers—owner informed, separate shutdown question awaiting reply. Production migrations/flags remain unapplied/off. Directional brushing remains owner-accepted.

Updated 2026-09-09 (Toronto). **In progress. The complete approved plan is not delivered or ready for customers.** This is a working native-code foundation and an offline design preview, not a production-connected app.

**Resumed after restart:** the owner confirmed Chrome works and asked to continue. The saved Mobile checkpoint is `43773f5`; Main/Admin compatibility edits remain preserved on disk, uncommitted alongside other work. No mobile remote is configured. `RESTART_HANDOFF.md` preserves the earlier pause and current resumption notes.

## Latest verification — ordinary Studio submission recovery

**14:44 Toronto update:** implemented bounded, account/environment-scoped native receipts before job dispatch, exact read-only server reconciliation across all nine services, and acknowledgment only after the matching owned job renders. Unknown responses and account changes never auto-replay a paid request; corruption/duplicate matches stay explicit. See `SUBMISSION_RECOVERY.md`. **923 Mobile tests / 54 files**, TypeScript/contracts passed; **271 customer API tests / 24 files**, targeted lint and dry-run bundle passed. API TypeScript still has the same four pre-existing reference-furniture diagnostics. The new API flag is **disabled**, generated types are updated, no migration added and no deployment occurred. Source inventory remains 123 tables/unchanged hashes. Final beta Release/configuration build: `output/ios-simulator-2026-09-09T18-41-41.796Z.log`; installed JS SHA256 `edbb43fc2d441c888e5d5e7063f64dbfac82643aeecbef7c63f82e90ede6d4a7`. Native demo startup is verified; this is not staging recovery-screen/Keychain acceptance. Physical-device, audited unresolved-receipt resolution and backend rollout gates remain. Android's same four packages remain missing at 14:37; no Mobile remote; Docker owner-stopped; 21 earlier migrations unapplied. Owner-accepted brushing was not repeated. No production, network or imaging Worker changes.

Final recovery-build smoke at 14:47: installed JS hash matches; sample login/Home/project/result/Back passed, simulator left on Home, PID 61325 alive, no new Furnio crash report in the bounded check. No build or new listener remains. This is native demo UI evidence only; the live receipt and reconciliation gates above remain open.

## Previous verification — upload/session fencing

**14:17 Toronto update:** Studio and Batch upload chains now remain tied to their initiating user/Auth session, stop after unmount/account changes, preserve same-session token refresh and abort on sign-out start. Signed storage requests never receive customer credentials; file-size/completion matching, request deadlines and late-response guards are tested. **892 tests / 53 files**, TypeScript, contracts and diff check passed. Beta Release/configuration build `output/ios-simulator-2026-09-09T18-13-01.094Z.log` is installed with matching executable/JS hashes. Native Studio stock-photo → sample result → Back and two-photo reviewed batch submission passed; Home is open. Bounded check found no new Furnio crash; dependency warnings remain. See `UPLOAD_SESSION_SAFETY.md` for exact scope, security assumptions and limitations. **Next: durable single-job uncertain-submission recovery**, then picker/SDK cache ownership and dialog lifetime. Real staging/device/commerce/deletion gates are not passed. Android's four exact packages remain missing at 14:13; no Mobile remote. No production, web/Admin/Worker, network or Docker changes.

## Previous verification — native service inputs and disclosure crash fix

**13:56 Toronto update:** all-nine-service preflight and real-ID request assembly, reference append/remove/pin controls and hidden-disclosure validation are implemented. Native testing exposed a glyph-context crash and an offscreen measurement failure; the pinned SVG patch now serializes measurement on the UI thread and prepares logical bounds with a one-pixel render. **846 tests / 50 files**, TypeScript and contracts passed. Beta Release/configuration build `output/ios-simulator-2026-09-09T17-46-48.797Z.log` is installed with matching executable/JS hashes. The final build passed the reference demo round trip, inspected 4032 × 3024 long-label and no-label JPEG exports, re-enabled-field validation, repeated font/position updates and cancelled-share cleanup. No new crash appeared in the bounded check; beta/dependency/framework diagnostics remain. See `STUDIO_INPUT_NATIVE_QA.md` for the actual crash, intermediate failed export, exact evidence and limits. Home is open; no build/listener is left running. No web/Admin/Worker/production/network/Docker changes. Android's four packages were still absent at 13:47. Next priority is reviewing ordinary Studio asynchronous-operation/account-change fencing; broader native/staging/commerce/deletion gates remain unfinished.

## Previous verification — result export restart recovery

**13:15 Toronto update:** native result exports now persist scoped file ownership, recover abandoned/partial copies at startup/foreground, conservatively retain interrupted shares for 24 hours, and expose non-blocking cleanup retry. Unknown files and originals remain protected. **798 tests / 49 files**, TypeScript and contracts passed; beta Release/configuration build `output/ios-simulator-2026-09-09T17-11-12.106Z.log` passed and is installed with matching JS hash. Native checks passed normal cancellation/cleanup, app termination during sharing, restart retention, injected-expiry/partial-write cleanup and unknown-file warning/retry without blocking sample entry. See `EXPORT_RESTART_RECOVERY.md` for exact evidence, synthetic fixture details and remaining SDK/picker/batch/physical-device boundaries. App is on Home; no build/preview process was left running. Android's four packages were still missing at 13:15. No production, website, Admin, Worker, network or Docker changes.

## Previous verification — disclosure reliability and long-label fitting

**12:54 Toronto update:** added per-export cancellation/readiness handling, late/duplicate callback protection, exact PNG/decode/JPEG dimension checks, safe native-reference release, measured long-label fitting shared by preview/export, and clearer validation messages. Final beta Release/configuration build `output/ios-simulator-2026-09-09T16-49-34.710Z.log` passed and its installed JS hash matches. Native final-build checks passed 81-character rejection with the new explanation, recovery to a full-resolution fitted 80-character label, no-label export, native share cancellation and named-file cleanup. **745 tests / 47 files**, TypeScript and frozen contracts passed. See `DISCLOSURE_RENDER_HARDENING.md` for exact evidence and remaining native failure/SDK interruption/device gates. No website, Admin, backend, production, network or Docker changes; the complete app remains unfinished.

## Previous verification — native JPEG export and sharing

**12:33 Toronto update:** the demo result screen now accepts an explicitly labelled local test photo and asks for confirmation before a real local Save/Share. Native QA passed actual **4032 × 3024 JPEG** saving with and without disclosure, Georgia/top-left export styling, opening/cancelling the system share sheet, retention while sharing and named export-file cleanup afterward. Two new test JPEGs remain only in the dedicated simulator's Photos library; no image was sent externally or processed by AI. Source suite: **696 tests / 46 files**, TypeScript and frozen contracts passed. Beta Release/configuration build `output/ios-simulator-2026-09-09T16-23-56.015Z.log` passed and the installed JS hash matches. See `NATIVE_JPEG_EXPORT_QA.md` for exact scope, source boundary and remaining renderer fault-path/long-label/device/staging gates. Android's same four exact packages remain missing. No production, website, Admin, Worker, network or Docker changes occurred.

## Previous verification — native batch mask review

**12:15 Toronto update:** native Batch now saves actual per-photo mask reviews in the local demo, with an export lock and photo-keyed ownership/cleanup. Two-photo Item removal QA passed empty-selection rejection, per-photo review gating, retained points after switching, re-review invalidation/replacement, sample-only submission and explicit Back. The actual four PNG files had the correct distinct dimensions/paint; replacement removed only the old photo's pair, and leaving Batch left zero test mask files. See `BATCH_MASK_REVIEW.md` for scope and remaining custom/stress/device gates. Source suite: **687 tests / 45 files**, TypeScript and frozen contracts passed. Beta Release/configuration build `output/ios-simulator-2026-09-09T16-11-06.056Z.log` passed and is installed. Full-resolution JPEG/disclosure saving and sharing remain a separate next test. No production services, purchases, network settings or Docker state changed.

## Previous verification — native mask export and review

**12:03 Toronto update:** the new local **Review selection** sheet shows each region's actual PNG on the photo or in black and white, including the shared batch editor. The same validated exporter prepares real job masks; review itself makes no upload or processing request. Native tests exposed and fixed two iOS SVG export defects: Retina-scaled dimensions and blank output caused by hidden 1×1 rendering bounds. A pinned pnpm patch and stale-CocoaPods-path build guard keep these fixes reproducible. See `NATIVE_MASK_REVIEW.md` for the upstream artifact-404/cached Release build limitation.

The corrected beta Release build/configuration passed (`output/ios-simulator-2026-09-09T15-56-28.459Z.log`), was installed, and its native executable hash matches the build. Native checks passed for 2048×1536 visible mask pixels, point alignment, distinct region exports, retained painting after closing/reopening review and Undo dropping only the emptied second region. Automated drags produced points, not full strokes; manual large-shape export, batch masks, slider, disclosure export and physical-device/staging tests remain open. The owner's all-direction drawing acceptance stays passed. Full source suite: **672 tests / 43 files**, TypeScript and frozen contracts passed. No Main/Admin/Worker source, production, live billing or network settings changed in this native milestone.

## Previous verification — email deletion guard and gesture retest

**11:25 Toronto update:** added the disabled email deletion checkpoint documented in `ACCOUNT_EMAIL_GUARD.md`. The Email Worker's **33 tests / 3 files, TypeScript, targeted lint and local dry-run build passed**. The new twenty-first migration and authored SQL/concurrency cases remain **unexecuted** because Docker is still stopped by the owner; the prior twenty-migration pass is not a pass for this change. Source inventory now covers **123 tables**. Automation suppression and complete deletion remain unfinished. All gates stay disabled, with no production migration, deployment or email send.

The installed simulator bundle was independently compared with the rebuilt editor-fix bundle: both SHA256 values are `1e82c8e334132c1b0e42e1444804df92dcc5ccc7f20204fa5557c196e8de949a`. All 11 mask/navigation tests passed again. **The owner subsequently confirmed brushing works perfectly in all directions.** This passes directional mask drawing on the open Item removal photo in the iPhone simulator; export, batch masks, slider/Back/scroll restoration and physical devices remain separate acceptance gates. See `IOS_BETA_QA.md`.

## Previous verification — reporting browser QA after restart

**10:50 Toronto update:** owner manual testing established a directional brush/navigation conflict, not a blanket inability to paint. Studio/Batch now disable swipe dismissal only on those editor routes, preserve explicit Back, and pause page scrolling while a mask stroke is active. Release/cancel/unmount restore scrolling. Separate native result-export cleanup now handles partial writes and independent deletion failures without a stuck busy state, retains failed handles for in-screen retry, and does not delete a file during native sharing. Crash/restart cleanup is still unfinished; see `RESULT_EXPORT_LIFECYCLE.md`. **644 tests / 41 files**, TypeScript, frozen contracts and a fresh beta Release simulator build/configuration check passed (`output/ios-simulator-2026-09-09T14-50-10.775Z.log`). The updated demo was installed; post-fix human/native gesture acceptance and actual export remain pending. No Main/Admin/backend or production changes were made in this milestone.

**10:31 Toronto update:** Xcode beta's native Release simulator build and embedded configuration checks passed, and that exact app was installed/launched in Furnio Mobile QA. Home, wallet, native photo picking and region selection were inspected. Automated brush gestures did not produce visible paint, so mask drawing/export remain an explicitly open native acceptance test; see `IOS_BETA_QA.md`. Mobile's 625 tests, TypeScript and frozen contracts passed again. No live account, purchase, upload or generation was used.

Native billing signals now also feed Admin's existing Action Centre behind the same disabled rollout flags, using global counts and visible configuration/outage warnings. Desktop links and the added flag-gated mobile hamburger-menu entry navigate to and focus the real report. The actual Admin shell was tested with in-memory auth/API fixtures; desktop/mobile navigation, same-page reopening, normal-navigation focus reset, outage/recovery and 320px overflow checks passed. No requests left the two-resource localhost fixture. The preview tab/server were closed. Final Admin verification at about 10:34: **96 tests / 16 files, TypeScript, targeted reporting/route lint and production build passed**. Full Admin-shell lint has two pre-existing command-search diagnostics, reproduced from committed HEAD and not suppressed. See `NATIVE_ADMIN_REPORTING.md` for scope and remaining gates. No new migration or production change was made.

About 10:09 Toronto: the isolated Chrome DevTools tab successfully rendered the real Admin reporting and customer-billing components with synthetic responses. Desktop (1280px), phone (390px), tablet (768px) and narrow-phone (320px) checks found no horizontal overflow. Pagination, exact-customer opening, focus/return focus, error presentation and refresh recovery passed. A small Admin-only CSS fix keeps mobile Previous/Next buttons side by side with the page caption below. The browser logged only the two local fixture resources and no console warnings/errors. See `NATIVE_ADMIN_REPORTING.md` for the precise tested scope. Authenticated staging, full-Admin shell, store and physical-device gates remain.

Admin's **82 tests across 14 files, TypeScript and production build passed again** after the CSS change. Earlier Mobile/SQL results below were not rerun during this browser milestone. Android's four exact SDK dependencies remain missing. Android Studio was opened without editing the unrelated project it restored. The owner confirmed Xcode beta is the intended GUI: `/Users/alipashaamidi/Downloads/Xcode-beta.app` reports Xcode 27.0 beta 6, build 27A5252f. The GUI subsequently showed the exact Furnio workspace, both Furnio/Pods projects, the Furnio scheme and Furnio Mobile QA destination; Run was available and Stop disabled, so no GUI build was running. Regular Xcode 26.6's GUI reports OS incompatibility on this Mac; its global command-line selection was not changed. Future Furnio beta builds should use a per-command `DEVELOPER_DIR`, and must be newly verified rather than inheriting the old simulator build's pass.

## Latest milestone — separate native Admin reporting

About 09:20 Toronto: added a disabled Overview report with Apple/Google and currency-separated verified purchases, refund-cohort amounts, recorded subscription states and paginated customer follow-up. It never guesses unknown revenue, combines CAD/USD or fetches Stripe invoices for native payments. Existing Stripe labels become source-explicit only under rollout flags; misleading combined margin is suppressed in that mode. See `NATIVE_ADMIN_REPORTING.md`.

Verification at that earlier milestone: **625 mobile/backend tests (38 files), 82 Admin tests (14 files), mobile/Admin TypeScript, frozen contracts, targeted Admin lint/build and isolated SQL/concurrency tests passed**. There are **20 unapplied mobile migrations**, with all flags still false. The read-only account inventory remains 122 tables. The initially blocked browser checks subsequently passed in the bounded offline scope recorded above. No mobile UI/processing Worker change or native rebuild was needed for this Admin-only milestone. The latest installed iOS build remains the successful 08:57 Toronto draft-privacy build (`output/ios-simulator-2026-09-09T12-57-11.958Z.log`). Production is unchanged; this is not store/staging/release certification.

## What exists now

- Separate Expo / React Native / TypeScript repository at `/Users/alipashaamidi/Dev/Furnio Mobile`, branch `codex/mobile-foundation`, independent pinned pnpm dependencies. No remote is configured and nothing has been pushed.
- Actual Furnio artwork, cream/forest/terracotta theme, bundled fonts and photographs, readable native components, safe areas, keyboard handling, navigation and confirmation modals.
- Welcome/sign-in/signup, recovery, phone verification, Home, Projects, project detail, Create, nine service editors, processing/results, disclosure controls, Activity, Credits & plans, and Account screens.
- Service-specific masks, multi-view ordering/anchor selection, furniture references/placement pins and floor-plan selection. These are native controls, not an embedded website.
- Validated client adapters for existing customer APIs, PKCE/secure-storage authentication, native Apple sign-in, restricted Turnstile challenge, server-derived feature/cost display, project history, uploads and jobs. They have **not yet been exercised against a configured staging environment**.
- Account/environment-isolated encrypted draft metadata, removable owned photo copies, 48-hour expiry on retrieval/store opening, restore/discard UI and interruption-safe sign-out/account-switch cleanup. Purchase/deletion/batch receipts and originals are preserved. See `DEVICE_DRAFT_PRIVACY.md`; full temporary-cache cleanup and physical lifecycle acceptance remain. No background/resumable upload system is claimed.
- Native result save/share and on-device disclosure renderer implemented; full-resolution output still needs physical-device verification.
- RevenueCat **client-side integration** with UUID identity, platform-specific public keys, purchase/restore flow, server reconciliation requirement and actual store-price display. No client credit grant. Demo purchases update only local sample state.
- Disabled mobile Worker with validated customer identity via service binding, bounded response reads, redacted diagnostics, security challenge, signed RevenueCat webhook inbox/queue handling, independent RevenueCat verification, and gated billing/activity reads.
- Twenty-five **unapplied-to-production** additive migrations implement native commerce, shared-ledger attribution, purchase recovery, compatibility reads, device notifications, initial deletion stages, Admin reporting/overlap warnings, email/automation guards and native conversion of unstarted trials. All25 replay with the complete application schema; 19 behavior checks pass. The earlier 24-migration unit/concurrency suite uses a minimal fixture. Neither is a hosted staging or store acceptance pass. See `FULL_SCHEMA_REPLAY.md`.
- Purchase recovery now reads server-side RevenueCat customer-event history in durable pages; account-scoped restore/status routes, duplicate protection, known-refund awareness, review warnings and app status modals are implemented. Restore can work while acquisition is disabled. Actual store payloads, devices, history-retention behaviour and intent-linked purchase completion still need acceptance tests; see `NATIVE_RECOVERY.md` and `NATIVE_PURCHASE_INTENTS.md`.

Latest verification (about 08:28 Toronto): **587 mobile/backend tests across 35 files**, mobile/Worker TypeScript, frozen contracts, **63 Admin tests**, Admin TypeScript/targeted lint/build and isolated SQL/concurrency tests passed. Explicit SDK cancellation can now clear a checkout selection after independently gated fresh server checks; pending/unknown outcomes remain protected. Read-only Admin late-payment timing warnings are included. See `NATIVE_CANCELLATION_RECOVERY.md` for the deliberate risk boundary and actual-store release gates. Offline Admin phone/tablet checks passed with no overflow or browser errors; the sample verified-payment link still works. A fresh iOS simulator Release build/configuration check passed (`output/ios-simulator-2026-09-09T12-25-36.904Z.log`), was installed in the dedicated Furnio simulator, and its demo welcome screen was inspected. This is startup evidence, not store or physical-device certification. Previous unchanged customer/web verification (about 07:45) remains **236 API tests across 23 files and 50 web tests across 13 files**, web TypeScript and targeted API lint. The account-deletion source inventory now covers 122 tables. All nineteen mobile migrations remain unapplied and all rollout flags remain false. The same four unrelated API compiler diagnostics remain recorded; no shared release, real-store test or full-schema staging acceptance is claimed. Production is unchanged.
- Multi-view comparisons now use verified source-per-output metadata rather than guessed history/processing order. Stable asset-ID selection, source-load/expiry notices, account/job remounting and stale-refresh/image callback guards are implemented. Selected trial outputs retain their own protected preview. See `RESULT_COMPARISONS.md` for the local tests and staging/device gates.
- Lost Stripe Checkout creation responses now have bounded exact-account discovery, fresh session verification and an explicit website recovery/continue action, behind the existing disabled compatibility flag. Recovery never creates a second session or grants credits. Missing/ambiguous results stay protected. Uncertain protected Focus checkout creation preserves the original offer; only independently verified expiration releases it. See `STRIPE_CHECKOUT_RECOVERY.md`. This does not solve unknown/cancelled Apple/Google launches.
- The Auth sign-in block checkpoint is locally implemented behind independent disabled Worker/database flags. It uses the supported Auth admin API, exact account UUIDs, bounded/redacted responses, read-back and independent database evidence. It neither deletes Auth rows nor revokes sessions; it cannot mark deletion complete. Active administrators are excluded, and a permanent-fence trigger prevents racing promotions. No real account was banned. See `ACCOUNT_DELETION.md`.
- Disabled account-deletion implementation: fresh authentication-method proof, fixed-origin Auth verification, exact live session/AMR/MFA checks, minimal owned-file/billing-provider review and a durable interruption-safe request journal. First cleanup stages now fence work permanently, close API keys/webhook destinations, detach pushes and perform bounded exact-prefix R2 sweeps with retry leases. **The complete deletion workflow remains unfinished**, with no enabled button or completion claim. Identity/provider revocation, email/automation/personal-record cleanup, reserved-credit resolution, retention and staging acceptance remain. `ACCOUNT_DELETION.md` records the exact boundaries.
- Native Account privacy UI now connects a separate in-memory Supabase sign-in, exact-account/session checks, linked password/Google/Apple verification, existing TOTP/phone MFA and the device-only receipt journal. It is reachable without paid access or phone verification, and a signed-out user can check a saved minimal receipt. Normal login providers/callbacks are untouched. Final deletion remains code-blocked; sample demonstrations do not submit real requests.
- Disabled notification implementation: authenticated installation preparation/registration, device-only secure preference/capability storage, explicit opt-in, account-switch and offline sign-out cleanup, revision-protected disable, independent terminal-job outbox polling, generic lock-screen content, bounded retries, ticket/receipt handling and stale-token invalidation. No imaging trigger/Worker change. Staging, APNs/FCM/Expo credentials, physical devices, delivery monitoring and privacy acceptance remain; see `NOTIFICATIONS.md`.
- Disabled purchase-intent layer: per-account Stripe/Apple/Google subscription reservations, one-shot launch, encrypted device journal, exact server-verified payment linkage and pending-checkout Admin alerts. Unstarted eligibility-response recovery cannot cancel a launched checkout. Explicit SDK cancellation now has its own leased server risk checks and audit; it cannot grant credits or cancel a subscription. **Actual-store cancellation certification, unknown/pending launches, conflicting hints and non-uniquely-verifiable Stripe sessions remain release blockers.**
- Disabled website/API compatibility: provider-specific subscription management, native transaction history without Stripe invoice lookups, monthly Stripe checkout rejection for a known native subscriber, gated shared acquisition locking, and unchanged one-time top-ups. External store actions and older pre-rollout sessions still require cutover/reconciliation; no global zero-risk exclusivity claim.
- Disabled Admin compatibility: native-aware customer filters/provider badges and a customer panel with store subscriptions, actual purchase currencies/amounts (or “not verified”), pending/quarantined events and refund-credit shortfalls. Owner/admin/finance can audit-link an independently verified purchase; support stays read-only. Global financial dashboards, automation audiences and unresolved provider cancellation workflows still need native support.
- Native bulk editor for the seven services supported by the existing batch API, per-photo review/masks, one reservation before uploads, stop-on-error behaviour and account-specific encrypted batch intent recovery. No automatic retry of an uncertain paid request.

## Latest device-privacy milestone

About 08:55 Toronto: **625 mobile/backend tests across 38 files**, mobile/Worker TypeScript and frozen API contracts passed. Saved-draft cleanup has scoped photo copies, a resumable encrypted-piece journal, same-account restart preservation and stale-save/account-switch guards. The iOS Release simulator build/configuration passed and its installed offline welcome screen was inspected. This is not authenticated draft or physical-device certification. Main/Admin/Workers were not modified or deployed in this milestone. All nineteen migrations remain unapplied and flags remain false. Android’s four exact SDK prerequisites were rechecked and remain missing. See `DEVICE_DRAFT_PRIVACY.md` for the precise boundaries.

## Phase status

| Phase                   | Current state                                                                                                                      | Still required                                                                                                                                     |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Foundation           | Local repository, plan, contract snapshots, isolation and baseline unit tests established                                          | Confirm mobile remote; inventory deployed versions and isolated staging                                                                            |
| 2. Native UI            | Core screens, nine service editors and batch review UI implemented; local demo available                                           | Full native/device acceptance, accessibility and interruption testing                                                                              |
| 3. Customer integration | Client adapters and input validation implemented                                                                                   | Configure staging; real OAuth/SMS, uploads, ownership, prompt-snapshot parity, all service cases                                                   |
| 4. Native commerce      | Client SDK, verified webhook processor, durable history recovery, purchase-intent locks, native ledger, shared entitlements, web/Admin compatibility and separate native reporting implemented locally | Store configuration, actual lifecycle acceptance, unknown-launch resolution, Action Centre/automation compatibility, full-schema/staging and report visual tests |
| 5. Release readiness    | Native privacy/reverification/receipt UI, opt-in notification lifecycle/outbox, permanent work fence and first bounded media cleanup implemented locally | Complete identity/provider/personal-data/retention deletion workflow, real OAuth/MFA and receipt recovery acceptance, push configuration/device acceptance, disclosures, monitoring and end-to-end regressions |
| 6. Private testing      | Not started                                                                                                                        | Signed binaries, TestFlight and Google Play internal testing                                                                                       |
| 7. Public release       | Not started                                                                                                                        | Owner acceptance, store review, approved Canada/US rollout                                                                                         |

## Website protection status

Limited, additive website/API/Admin source edits implement the approved native-billing compatibility layer behind disabled server/database flags. Twenty-one new migration **files** were authored in Furnio main; they are unapplied. Shared-table guards/restrictive policies require staging latency and full-schema acceptance. No production database mutations, authentication/Stripe configuration changes, Worker deployments, paid model calls, real pushes or purchases were performed. Unrelated working-tree changes were preserved. Existing imaging consumer Workers and Admin prompt selection were not changed or deployed. The latest backend milestone also changes the email Worker behind its own disabled flag; it has not been deployed.

| Repository    | Recorded Git source                               | Recorded local revision                    |
| ------------- | ------------------------------------------------- | ------------------------------------------ |
| Furnio main   | `https://github.com/pashatech25/furnio-main.git`  | `19908507ecb032abaa38b07df49648e677dd880c` |
| Furnio Admin  | `https://github.com/pashatech25/furnio-admin.git` | `f9d6e913532ce36f75ddc6c627ab0b0f8eb84e65` |
| Furnio Mobile | No remote yet                                     | Local checkpoint; use `git log -1`; no push |

The main worktree already contained reference-furniture/upload changes, a research document and migrations (including the existing Stripe live-mode migration). They remain untouched. Admin has an existing untracked `.codex-tmp/` directory; it was not removed.

**Deployed release IDs have not been inventoried.** Local Git revisions must not be presented as deployed versions. Record Cloudflare/Vercel deployment IDs before any eventual shared release.

## Verified checks

Re-run commands from the indicated repository; counts reflect this milestone, not a coverage percentage.

| Check                                            | Result                                                                                                                                                                                                             |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Mobile `pnpm typecheck`                          | Passed                                                                                                                                                                                                             |
| Mobile `pnpm test`                               | Latest full run: **745 tests, 47 files passed** after disclosure capture, resolution, reference-lifetime and fitting changes |
| Mobile `pnpm check:account-inventory`             | Passed: 123 source table declarations across main/Admin, migration-file hashes checked; not live-schema or cleanup certification |
| Mobile `pnpm test:ledger`                        | Earlier twenty-migration synthetic suite passed. **Current twenty-first migration and new email SQL/concurrency cases not run: Docker stopped.** |
| Mobile `pnpm check:contracts`                    | Frozen schema checksums passed                                                                                                                                                                                     |
| Mobile `pnpm exec expo install --check`          | Compatible dependencies after aligning React types                                                                                                                                                                 |
| Mobile `pnpm worker:types` + `pnpm worker:check` | Generated current Worker bindings/runtime; typecheck passed                                                                                                                                                        |
| Mobile `pnpm worker:build`                       | Dry-run bundle passed; not deployed                                                                                                                                                                                |
| Mobile `pnpm export:preview`                     | Compiled web design preview exported                                                                                                                                                                               |
| Expo iOS/Android export                          | Both JS/Hermes bundles exported; these are **not installable native binaries**                                                                                                                                     |
| iOS native Release simulator build               | Latest arm64 beta build/configuration validation passed (9 September, 12:50 Toronto disclosure build). Limited mask and full-resolution JPEG/long-label export QA passed; owner accepted directional painting. Native sharing was opened/cancelled without sending. Physical-device, native fault injection and staging gates remain; not signed for devices/stores. |
| Android Expo prebuild                            | Generated native project; verified `singleTop` activity configuration                                                                                                                                              |
| Existing shared tests                            | **10 passed** (`pnpm --filter @virtual-staging/shared test`)                                                                                                                                                       |
| Customer API tests                               | **236 passed**, including optional result-source mapping/ownership/legacy responses and the prior credit confirmation, native billing and checkout protections (`pnpm --filter @virtual-staging/api test`) |
| Web tests                                        | **50 passed**, including native provider/history display and safe checkout recovery responses (`pnpm --filter @virtual-staging/web test`) |
| Admin tests                                      | **82 passed across 14 files**, including native reporting schema/decimal/currency separation, private role-gated reads, disabled rollout, redacted failure states and existing customer recovery tests |
| Admin build/typecheck                            | Passed local production build and TypeScript; not deployed                                                                                                                                                         |
| Web typecheck                                    | Passed after rebuilding the local shared package                                                                                                                                                                   |
| Main API typecheck                               | Blocked by four errors in untouched `reference-furniture-job.ts` / `.test.ts`; see below                                                                                                                           |
| Targeted new/modified billing-file lint          | Passed for API, web and Admin                                                                                                                                                                                      |

Mobile tests cover transport isolation/no automatic paid retries, all nine request schemas, unsafe prompt-field stripping, masks/geometry, disclosure placement, trial-vs-granted-credit presentation, UTF-8 secure storage, account-isolated drafts and disabled Worker authorization/security behaviour.

Browser smoke checks opened all nine service editors and exercised demo navigation and sample purchase UI. The browser is a preview of native components, **not evidence of native file export, Apple/Google billing or physical-device behaviour**. Initial preview QA exposed empty-string React Native rendering and a multi-output source-pairing issue; both were corrected.

The final compiled preview was also checked at 390×844 (phone) and 768×1024 (tablet), with clean console output on the tested home → project → result/disclosure flow. Tablet Home has no horizontal overflow (document width 768). These limited smoke checks are not a complete accessibility or responsive-layout audit.

The later batch smoke test selected two local fixture photos, retained different room settings, required both per-photo reviews and confirmed sample-only submission. Its document width matched 390px and 768px viewports, with zero console errors. Screenshots were inspected under `output/playwright/` (ignored by Git). Native masks and real reserved batch processing remain device/staging gates.

## Known build/tooling limitations

- iOS 26.5 runtime is installed and Furnio Mobile QA booted successfully. Native arm64 Release build, installation and demo startup now pass. Initial build/startup tests exposed generated bundle/EXConstants path-quoting and `basename` bugs for `Furnio Mobile`. The reproducible local config plugin fixes the script invocations; `app.config.ts` explicitly preserves RN's required new-architecture flag because its upstream CocoaPods `find` also splits paths with spaces. Dependency sources were not hand-patched. Real Expo configuration generation and final embedded-manifest checks now pass. Upstream dependency deprecation/nullability/script-phase warnings remain; no warning-free claim is made. Physical-device signing and store distribution remain unverified.
- Android installation progressed during this session: API 36.1, Build-Tools 36.1.0/37.0.0, Platform-Tools, Emulator and three AVDs exist. Exact app build dependencies API 36.0, Build-Tools 36.0.0, NDK 27.1.12297006 and command-line tools are still absent. No device is running and no APK/AAB was built. See `LAPTOP_SETUP.md`.
- The installed Expo/Metro development server exhibited an HMR root-URL crash (`jsc-safe-url`, empty path). A compiled local-only preview server was added instead of modifying dependency code or hiding errors. Use `pnpm export:preview && pnpm preview` for UI review; investigate HMR separately before relying on it for development.
- Physical-device camera, file picking, secure-session recovery, mask export, full-resolution disclosure rendering, native sharing and dynamic type have not passed device QA.
- Main API full TypeScript currently reports four errors in untouched `src/lib/reference-furniture-job.ts` / `.test.ts`: nullable string, Uint8Array/BlobPart and two `R2`-on-`never` test errors. `scripts/check-api-compatibility-baseline.mjs` proves the same four diagnostics remain when only this native billing slice is removed **in memory**, without editing main's unrelated working-tree changes. API unit tests and its local dry-run bundle passed, but this typecheck blocker must be resolved/reviewed before a shared release.

The actual web native-billing component and Admin read-only detail renderer were visually inspected with clearly labelled offline sample data at 390×844 and 768×1024. Both matched viewport/document width, and the tested browser session ended with zero console errors/warnings. This is component QA, not authenticated production billing or native store purchase evidence.

Earlier native iOS simulator smoke QA opened the welcome/login demo, sample Home, project listing/detail, before/after result, disclosure controls and keyboard, demo export explanation, all nine service choices and multi-view editor. That earlier build did not actually save/share photos. The newer, explicit local-photo test path has since passed the bounded JPEG/Save/share-sheet checks documented in `NATIVE_JPEG_EXPORT_QA.md`. No real login, store purchase or paid model request was attempted. Physical-device and complete nine-service upload/mask/output tests remain open.

Reproduce the local native checks with `node scripts/verify-ios-config.mjs` and `node scripts/build-ios-simulator.mjs` after prebuild/CocoaPods. The latter writes a timestamped build log under ignored `output/`, validates `EXConstants.bundle/app.config`, and never signs/submits to a store. Use Expo 57's `--no-clean --no-install --platform ios` flags when incrementally regenerating existing native files; its default prebuild recreates the generated directory.

## Important remaining functional gaps

See `API_CONTRACT.md` for exact routes and integration boundaries.

1. **Native financial backend is implemented locally but not production-ready.** Purchase/restore history recovery now exists; actual sandbox certification, purchase-intent linkage, refund-reversal certification, cross-provider acquisition locks and store configuration remain. See `NATIVE_BACKEND.md` for implemented boundaries and release blockers.
2. **Initial website/Admin provider compatibility exists but is disabled/unreleased.** Provider/currency-separated purchase reporting and Action Centre signals are implemented locally. Current-recurring-price native MRR, automation “unsubscribed” audiences and the complete reconciliation recovery workflow still need native support. This compatibility layer must pass full-schema/staging regression gates before native sales. Never insert Apple/Google identifiers into Stripe columns.
3. **Native refunds/concurrency/rollover passed isolated SQL tests.** Full-schema staging, actual store sandbox, deployed webhook delivery and website end-to-end regression tests remain mandatory. Synthetic fixtures do not replace those gates.
4. **Complete account deletion is unfinished.** Native privacy/reverification/receipt UI, recent-auth review, permanent work fencing and first exact-prefix media cleanup are implemented and tested locally. Source inventory covers 123 table declarations. An initial email guard is authored with passing Worker tests but pending SQL execution. Provider/Auth revocation, full email/automation suppression, personal-data cleanup, financial/backup retention and completion remain. Real identity/MFA and secure-receipt lifecycle require staging/device acceptance. Push remains disabled/unconfigured. The UI's final destructive action remains disabled by server/code gates.
5. **Batch editor/reservation handling is implemented, not device-certified.** Per-photo masks, stop/partial failure and local recovery are present. Full background/resumable uploads and physical-device QA remain. Dedicated multi-view/reference workflows are not treated as generic batches.
6. **Activity now uses a gated, cursor-based owned-job API.** Its migration and flag are unapplied/disabled; the app explains unavailable browsing instead of silently truncating to ten projects.
7. **Multi-view source-per-result comparisons are implemented locally, not activated.** Stored job/step/source identity, ownership, exact preview keys, retention, stable selection and trial protection are tested. The independent flag is disabled; real staging pixels/ownership and physical-device acceptance remain. Missing metadata shows a result without comparison. See `RESULT_COMPARISONS.md`.
8. **Expected-cost protection is locally implemented but awaits staging acceptance.** Required versioned mobile aliases reject changed prices or paid/trial mismatches before paid work, and batch jobs use their reserved unit price. The independent flag is disabled. See `CREDIT_COST_CONFIRMATION.md`.
9. **Saved-draft cleanup is implemented; full device cleanup/acceptance remains.** Saved metadata/copies clear on sign-out/account switching with retry journals and preserved recovery receipts. Unknown legacy records, picker/result/export caches and confirmed-deletion closeout still require their separate policy/implementation. Notification unregister, offline cleanup, token rotation and receipts exist locally; physical-device/outage acceptance remain.
10. **Live billing presentation/management needs acceptance.** The mobile billing endpoint and multi-provider display exist, but the read flag is disabled and migrations are unapplied. The complete cross-provider purchase guard is not implemented; acquisition remains disabled in both Worker capabilities and database settings.

## Next execution sequence

1. Confirm a separate staging Supabase project and the matching API/processing Workers, or approve provisioning them. Never connect this test build to the production customer database as a shortcut.
2. Record staging/production deployment identifiers and customer-package snapshots; confirm the new mobile Git remote.
3. Configure additional native OAuth redirects/Apple IDs and the staging Turnstile host. Exercise existing-customer login and phone flow without changing web providers/callbacks.
4. Finish batch/history/cost/recovery gaps and test the existing processing contract against staging with provider calls mocked. Seek explicit approval before any paid FAL generation.
5. Certify restore/backfill and certify implemented refund reversals and finish acquisition locks, shared entitlement and web/Admin compatibility around the implemented native ledger/processor. Keep acquisition disabled; replay the complete schema and regression cases in isolated staging before shared deployment.
6. Sign into Apple, Google and RevenueCat; approve product prices/availability and agreements. Use sandbox only, test cross-account restores, duplicate delivery and refunds.
7. Implement shared-account deletion independently from imaging. Configure/certify the implemented push pipeline and finish privacy/retention disclosures. Install Android SDKs and obtain physical-device acceptance.
8. Only then prepare private builds and, after owner approval, public submissions.

No flag should be enabled merely to make a placeholder endpoint appear to work. The definition of done remains the one in the approved plan.
