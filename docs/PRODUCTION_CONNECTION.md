# Owner-requested production connection — September 9, 2026

The owner explicitly replaced the staging-test request with a production connection. Do not silently return either app to staging.

## Active targets

- Native configuration: `.env.production-native.local` (ignored, public values only), generated by `node scripts/prepare-production-native.mjs --production`.
- Customer API: `https://platform.furnio.ai`.
- Authentication: `https://auth.furnio.ai`, existing production project `sgsjkgfwgxmlqcgyuyeh`.
- Mobile support: `https://mobile-api.furnio.ai`; challenge is `/v1/challenge` on that host, not the website.
- Purchases remain website-only. Native commerce, notification delivery and permanent account cleanup flags remain disabled.

## Production changes actually applied

- Auth: appended the three `furnio://` callback URLs and enabled Apple audience `ai.furnio.app`. Verified all other Auth configuration fields unchanged. No customer identity/phone/credit records changed.
- Applied only `20260909051332_mobile_activity_read.sql`, with short transaction lock/statement timeouts. Read-only activity RPC denies direct anonymous/authenticated execution; support Worker first verifies the customer with the existing API. No native-commerce migrations applied.
- Customer API compatibility release built in `output/production-api-release`, a separate worktree at the previously deployed commit `203f4d1`. Only customer mobile quotes/routes, read-only submission recovery and result-source access were added, plus the exact owned mobile Turnstile hostname behind `MOBILE_TURNSTILE_ENABLED`. Existing webhook, Stripe and imaging implementations were not replaced with the unrelated working-tree changes.
- API Worker version: `4d656a05-100b-4ba5-996b-cc36f2ebf29e`.
- Support Worker version: `d40d0fc1-4c41-4e1b-9a97-4f75649b38f9`.
- Existing secret bindings retained. Support credential is server-only in an ignored owner-protected file; never embedded in native builds.
- An initial website challenge route was removed after it returned the Vercel 404. No website DNS/proxy change was made. The app now uses the independently hosted mobile API challenge.

## Evidence and limits

### Follow-up at 23:38 local time

- Full mobile TypeScript check passed; all 1,094 tests in 66 files passed.
- Observed a completed real login on the simulator: production home displayed the Budget account and three existing property projects. Displayed balance was 0; no credits were changed.
- Owner subsequently switched Google accounts. Do not interrupt the active sign-in or assume the earlier account remains selected.
- Confirmed the bundled Mapbox token returns HTTP 403 for a native-style static map request without website headers. Mapbox account sign-in is open for the owner; a separate public mobile token is still needed. Do not spoof website request headers or weaken the website token restrictions.

- Exact API release: 173 tests passed. Support/auth/config targeted tests: 42 passed. Production environment isolation: 4 tests passed.
- Production API health returns 200; unauthenticated mobile processing returns 401 (not the former 404).
- Corrected production builds compiled and installed in iOS QA simulator and Android emulator. Android SHA256: `9c9a24fd459f8e52aec1c929c06826048d78fb867f2a9b7fab1b5e6133ab0990`. iOS sign-in visually inspected after installation: no staging banner or prior security-service error. Actual authenticated sign-in/Turnstile redemption awaits owner input.
- Mobile challenge returns 200 with the real widget document. Production API binding comparison confirms no missing original bindings; only the four mobile flags were added.
- API whole-project TypeScript still reports pre-existing errors in unchanged reference-furniture files; do not report a clean whole-project typecheck.
- No agent-initiated paid generation was run and no production credits granted or modified. Real end-to-end generation must be observed with the signed-in owner's submission before claiming it passed.
- Production login/account display, real upload/generation/result/export and all-nine-service coverage require observed results; compilation is not that evidence.
- Native Mapbox token restrictions remain to be verified/fixed; existing website token must not be changed.
- This connection is not proof of store submission readiness, physical-device installation, push notifications or account deletion completion.

Build commands: `node scripts/build-ios-simulator.mjs --production` and `node scripts/build-android-emulator.mjs --production`. Do not publish these emulator/simulator artifacts as store releases.
